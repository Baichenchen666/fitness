<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.graduate.mapper.CoachMapper">
    
    <sql id="table_name">coach</sql>
    
	<sql id="coachColumns">
		a.id AS "id",
		a.name AS "name",
		a.gender AS "gender",
		a.phone AS "phone",
		a.type AS "type",
		a.info AS "info",
		a.background AS "background",
		a.qrcode AS "qrcode",
		a.num AS "num"
	</sql>
	
	<sql id="coachJoins">
	</sql>
    
    <!-- 根据id查询sql语句 -->
	<select id="get" resultType="com.graduate.bean.Coach">
		SELECT 
			<include refid="coachColumns"/>
		FROM <include refid="table_name"/> a
		<include refid="coachJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<!-- 查询列表sql语句 -->
	<select id="findList" resultType="com.graduate.bean.Coach">
		SELECT 
			<include refid="coachColumns"/>
		FROM <include refid="table_name"/> a
		<include refid="coachJoins"/>
		<where>
			
			<if test="coach.name != null and coach.name != ''">
				AND a.name LIKE concat('%',#{coach.name},'%')
			</if>
			<if test="coach.info != null and coach.info != ''">
				AND a.info = #{coach.info}
			</if>
			<if test="coach.type != null and coach.type != ''">
				AND a.type = #{coach.type}
			</if>
		</where>
		ORDER BY a.id ASC
	</select>
	
	<!-- 新增sql语句 -->
	<insert id="insert" parameterType="com.graduate.bean.Coach">
		INSERT INTO <include refid="table_name"/>(
			id,
			name,
			gender,
			phone,
			type,
			info,
			background,
			qrcode,
			num
		) VALUES (
			#{id},
			#{name},
			#{gender},
			#{phone},
			#{type},
			#{info},
			#{background},
			#{qrcode},
			#{num}
		)
	</insert>
	
	<!-- 修改sql语句 -->
	<update id="update" parameterType="com.graduate.bean.Coach">
		UPDATE <include refid="table_name"/> SET 	
			name = #{name},
			gender = #{gender},
			phone = #{phone},
			type = #{type},
			info = #{info},
			background = #{background},
			qrcode = #{qrcode},
			num = #{num}
		WHERE id = #{id, jdbcType = VARCHAR}
	</update>
	
	<!-- 删除sql语句 -->
	<update id="delete">
		DELETE FROM <include refid="table_name"/>
		WHERE id = #{id}
	</update>
	
</mapper>